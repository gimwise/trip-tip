import React from 'react';

const CalculatePage = () => {
    return (
        <div>
            그룹 - 멤버 편집 버튼, 영수증 편집 버튼 , 1/N(보기, 편집), 직접 입력(보기, 편집)
        </div>
    );
};

export default CalculatePage;